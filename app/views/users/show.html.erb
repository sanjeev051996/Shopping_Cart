<%= "Hey Admin!, manage the site please" if @user.admin? %>
<p>
  <strong>NAME:</strong>
  <%= @user.name %>
</p>
 
<p>
  <strong>EMAIL:</strong>
  <%= @user.email %>
</p>
<p>
  <strong>DOB:</strong>
  <%= @user.dob %>
</p>
<p>
  <strong>Phone:</strong>
  <%= @user.phone %>
</p>
<p>
  <strong>ADDRESS:</strong>
  <%= @user.address %>
</p>
<p>
  <strong>COUNTRY:</strong>
  <%= @user.country %>
</p>
<p>
  <strong>STATE:</strong>
  <%= @user.state %>
</p>
<h2> Past Orders </h2>
<%= render @user.orders %>
<br>
<%= link_to 'Users List', users_path if @user.admin? %>
<%= link_to 'Settings', update_profile_users_path  if current_user == @user %>
<%= link_to 'Change profile details', edit_user_path(@user) if current_user.admin? && !@user.admin?  %>
<%= link_to 'Add Products', new_product_path if @user.admin? %>
<%= link_to 'Your Cart', display_carts_path if current_user == @user %>
<%= link_to 'Manage Orders', orders_path if @user.admin? %>
<%= link_to 'View all products', products_path if current_user == @user %>
<%= link_to 'Log out', logout_sessions_path, method: :delete  if current_user == @user %>
<%= link_to 'Back to Admin Profile', profile_users_path unless current_user == @user %>
<br>
<p>Time now is <%= Time.now %> </p>
<p> You have logged in <%= session[:times_visited] %> times </p>
